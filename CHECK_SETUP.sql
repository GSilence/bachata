-- ============================================
-- ПРОВЕРКА НАСТРОЙКИ БАЗЫ ДАННЫХ
-- ============================================
-- Выполните эти команды, чтобы проверить, что всё настроено правильно

-- 1. Показать все базы данных (должна быть bachata_db)
SHOW DATABASES;

-- 2. Показать информацию о пользователе
SELECT user, host, plugin, authentication_string IS NOT NULL as has_password 
FROM mysql.user 
WHERE user = 'bachata_user';

-- 3. Показать права пользователя на базу bachata_db
SHOW GRANTS FOR 'bachata_user'@'localhost';

-- 4. Попробовать подключиться к базе (должно работать без ошибок)
USE bachata_db;
SELECT DATABASE(); -- Покажет текущую базу данных

-- 5. Проверить, что можем создавать таблицы (Prisma это сделает автоматически)
SHOW TABLES; -- Пока должно быть пусто, это нормально

