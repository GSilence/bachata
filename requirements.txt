# Python dependencies for Bachata Beat Counter

# Build dependencies (нужны для компиляции madmom)
Cython>=0.29.0
setuptools>=65.0.0
wheel>=0.38.0

# Audio analysis
madmom>=0.16.1
# Essentia не устанавливается на Windows (требует компиляцию C++)
# На Windows автоматически используется librosa fallback (уже установлен)
# Для Linux/Mac можно установить вручную: pip install essentia
numpy>=1.24.0
scipy>=1.10.0

# Audio loading (используется для загрузки аудио файлов)
librosa>=0.10.0
soundfile>=0.12.0

# Demucs для разделения на стемы
demucs>=4.0.0

