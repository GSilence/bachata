# Установка Madmom для анализа ритма бачаты

## Описание

Скрипт `scripts/analyze_beat.py` использует библиотеку **Madmom** для более точного определения сильных долей (downbeats) в музыке бачаты. Madmom использует нейронные сети для детекции ритма и значительно превосходит librosa в сложных композициях.

## Установка зависимостей

### Windows

1. **Установите C++ Build Tools (обязательно!):**

   📖 **Подробная инструкция:** См. [docs/INSTALL_CPP_BUILD_TOOLS.md](INSTALL_CPP_BUILD_TOOLS.md)

   **Кратко:**

   - Скачайте: https://visualstudio.microsoft.com/visual-cpp-build-tools/
   - При установке выберите **"C++ build tools"**
   - Дождитесь завершения (~20-40 минут)
   - **Перезапустите терминал**

2. **Установите системные библиотеки (если еще не установлены):**

   ```powershell
   # FFmpeg (требуется для madmom)
   # Скачайте с https://ffmpeg.org/download.html
   # Или используйте chocolatey:
   choco install ffmpeg
   ```

3. **Активируйте виртуальное окружение:**

   ```powershell
   .\venv\Scripts\Activate.ps1
   ```

4. **Установите build-зависимости:**

   ```powershell
   # Сначала установите Cython и другие build-инструменты
   pip install Cython>=0.29.0 setuptools>=65.0.0 wheel>=0.38.0
   ```

5. **Установите Python зависимости:**

   ```powershell
   pip install -r requirements.txt
   ```

   Или установите вручную:

   ```powershell
   # Сначала build-зависимости
   pip install Cython setuptools wheel

   # Затем остальные
   pip install madmom>=0.16.1
   pip install numpy>=1.24.0
   pip install scipy>=1.10.0
   pip install librosa>=0.10.0
   pip install soundfile>=0.12.0
   ```

### Linux / macOS

1. **Установите системные библиотеки:**

   **Ubuntu/Debian:**

   ```bash
   sudo apt-get update
   sudo apt-get install ffmpeg libsndfile1
   ```

   **macOS (с Homebrew):**

   ```bash
   brew install ffmpeg libsndfile
   ```

2. **Активируйте виртуальное окружение:**

   ```bash
   source venv/bin/activate
   ```

3. **Установите Python зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

## Проверка установки

Проверьте, что madmom установлен корректно:

```bash
python -c "from madmom.features.beats import RNNDownBeatProcessor; print('Madmom OK')"
```

Если вы видите "Madmom OK", установка прошла успешно.

## Использование скрипта

```bash
python scripts/analyze_beat.py path/to/audio.mp3
```

Скрипт выведет JSON в stdout:

```json
{
  "bpm": 123.0,
  "offset": 0.5,
  "beats": [
    {"time": 0.5, "number": 1},
    {"time": 1.0, "number": 2},
    {"time": 1.5, "number": 3},
    ...
  ]
}
```

## Устранение проблем

### Ошибка: "madmom not available"

**Решение:** Убедитесь, что:

1. Виртуальное окружение активировано
2. Madmom установлен: `pip install madmom`
3. FFmpeg установлен и доступен в PATH

### Ошибка: "FFmpeg not found"

**Решение:**

- Windows: Добавьте FFmpeg в PATH или установите через chocolatey
- Linux: `sudo apt-get install ffmpeg`
- macOS: `brew install ffmpeg`

### Ошибка: "ModuleNotFoundError: No module named 'Cython'"

**Решение:** Установите build-зависимости перед установкой madmom:

```powershell
pip install Cython setuptools wheel
pip install madmom
```

### Ошибка: "cannot import name 'MutableSequence' from 'collections'"

**Проблема:** В Python 3.10+ классы из `collections.abc` были перемещены, а madmom 0.16.1 использует старый синтаксис импорта.

**Решение:** Патч для совместимости уже включен в скрипты. Он автоматически применяется при запуске скриптов.

### Ошибка: "module 'numpy' has no attribute 'float'"

**Проблема:** В NumPy 1.20+ устаревшие алиасы `np.float`, `np.int`, `np.bool` были удалены, а madmom 0.16.1 их использует.

**Решение:** Патч для совместимости уже включен в скрипты. Он автоматически добавляет обратную совместимость для этих типов:
- `np.float` → `np.float64`
- `np.int` → `np.int64`
- `np.bool` → `np.bool_`
- `np.complex` → `np.complex128`

Патчи применяются автоматически при запуске скриптов `analyze-track.py`, `analyze_beat.py` и `visualize_madmom.py`.

### Ошибка: "Microsoft Visual C++ 14.0 or greater is required"

**Проблема:** Madmom требует компилятор C/C++ для компиляции из исходников. На Windows это требует установки Visual Studio Build Tools (~3-6 GB).

**Решение:** Madmom обязателен для работы скрипта. Установите Visual Studio Build Tools:

📖 **Подробная инструкция:** См. [docs/INSTALL_CPP_BUILD_TOOLS.md](INSTALL_CPP_BUILD_TOOLS.md)

**Кратко:**

1. Скачайте с https://visualstudio.microsoft.com/visual-cpp-build-tools/
2. При установке выберите **"C++ build tools"**
3. Установите (может занять 20-40 минут)
4. **Перезапустите терминал** (важно!)
5. Попробуйте снова: `pip install madmom`

**Альтернатива:** Попробуйте использовать предкомпилированные wheels (если доступны):

```bash
pip install --only-binary :all: madmom
```

_Примечание: для Windows предкомпилированные wheels могут быть недоступны_

## Требования к системе

- Python 3.8+
- FFmpeg (для обработки аудио)
- 2GB+ свободной RAM (для обработки больших файлов)
- ~500MB дискового пространства (для моделей madmom)

## Дополнительная информация

- [Документация Madmom](https://madmom.readthedocs.io/)
- [Репозиторий Madmom на GitHub](https://github.com/CPJKU/madmom)
