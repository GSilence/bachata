# Установка на уже созданный VDS

Если ваш VDS уже создан, cloud-init не сработает (он работает только при первом запуске). Используйте один из вариантов установки ниже.

## 🚀 Рекомендуемый способ: Автоматический скрипт

Используйте улучшенный скрипт `install-bachata.sh` с автоматическими проверками:

```bash
# Скачайте скрипт
wget https://raw.githubusercontent.com/GSilence/bachata/master/install-bachata.sh -O install-bachata.sh

# Или через curl
curl -o install-bachata.sh https://raw.githubusercontent.com/GSilence/bachata/master/install-bachata.sh

# Сделайте исполняемым и запустите
chmod +x install-bachata.sh
./install-bachata.sh
```

**Преимущества:**

- ✅ Автоматически проверяет, что уже установлено
- ✅ Пропускает выполненные шаги
- ✅ Можно запускать многократно
- ✅ Подробное логирование

**Или используйте оригинальный скрипт `install-on-existing-vds.sh` (см. ниже):**

## Быстрая установка

### Вариант A: Через веб-консоль сервера (рекомендуется)

**Шаг 1:** Откройте веб-консоль в панели Timeweb (VNC/Console)

**Шаг 2:** Создайте файл скрипта:

```bash
nano /root/install-on-existing-vds.sh
```

**Шаг 3:** Скопируйте **весь** содержимое файла `install-on-existing-vds.sh` с вашего компьютера и вставьте в редактор (в nano: правый клик или Shift+Insert для вставки)

**Шаг 4:** Сохраните файл:

- В nano: `Ctrl+O` (сохранить), затем `Enter`, затем `Ctrl+X` (выйти)

**Шаг 5:** Запустите установку:

```bash
chmod +x /root/install-on-existing-vds.sh
/root/install-on-existing-vds.sh
```

### Вариант B: Через SSH клиент

**Шаг 1:** Загрузите скрипт на сервер с вашего компьютера:

```bash
scp install-on-existing-vds.sh root@your-server-ip:/root/
```

Или через SFTP клиент (FileZilla, WinSCP и т.д.)

**Шаг 2:** Подключитесь к серверу:

```bash
ssh root@your-server-ip
```

**Шаг 3:** Запустите установку:

```bash
chmod +x /root/install-on-existing-vds.sh
/root/install-on-existing-vds.sh
```

### Вариант C: Через wget/curl (если скрипт в репозитории)

Если скрипт размещен в Git репозитории или на веб-сервере:

```bash
# Скачать напрямую
wget https://raw.githubusercontent.com/GSilence/bachata/master/install-on-existing-vds.sh -O /root/install-on-existing-vds.sh

# Или через curl
curl -o /root/install-on-existing-vds.sh https://raw.githubusercontent.com/GSilence/bachata/master/install-on-existing-vds.sh

# Затем запустить
chmod +x /root/install-on-existing-vds.sh
/root/install-on-existing-vds.sh
```

Установка займет **15-30 минут** (особенно компиляция madmom).

## Что делает скрипт

Скрипт устанавливает все то же самое, что и cloud-init:

✅ Все системные зависимости (Node.js, Python, FFmpeg, MySQL, build tools)  
✅ Python виртуальное окружение с madmom, demucs, librosa  
✅ MySQL базу данных  
✅ Systemd сервис для автозапуска  
✅ Nginx как reverse proxy  
✅ Firewall  
✅ Скрипт управления `bachata-manage`

## После установки

### 1. Загрузите проект на сервер

```bash
# Через Git
cd /opt/bachata
sudo -u bachata git clone https://github.com/GSilence/bachata.git .

# Или через SCP с вашего компьютера
scp -r ./* root@your-server-ip:/opt/bachata/
ssh root@your-server-ip "chown -R bachata:bachata /opt/bachata"
```

### 2. Выполните финальные команды

```bash
cd /opt/bachata
sudo -u bachata npm install
sudo -u bachata npm run db:generate
sudo -u bachata npm run db:push
sudo -u bachata npm run build
systemctl start bachata-beat-counter
```

### 3. Загрузите голосовые файлы

Загрузите файлы `1.mp3` - `8.mp3` в `/opt/bachata/public/audio/voice/`:

```bash
# С вашего компьютера
scp public/audio/voice/1.mp3 public/audio/voice/2.mp3 ... root@your-server-ip:/opt/bachata/public/audio/voice/

# На сервере установите права
chown -R bachata:bachata /opt/bachata/public/audio/voice
```

**Важно:** Приложение использует только файлы 1-8.mp3 для голосового счета битов.

### 3. Проверьте работу

```bash
# Статус
bachata-manage status

# Логи
bachata-manage logs

# Доступ к приложению
# Откройте в браузере: http://your-server-ip
```

## Важные файлы

- **Лог установки**: `/var/log/bachata-install.log`
- **Пароль БД**: `/root/db_credentials.txt` (показывается также в конце установки)
- **Приложение**: `/opt/bachata`
- **Конфигурация Nginx**: `/etc/nginx/sites-available/bachata-beat-counter`

## Управление

```bash
bachata-manage start    # Запустить
bachata-manage stop     # Остановить
bachata-manage restart  # Перезапустить
bachata-manage status   # Статус
bachata-manage logs     # Логи
bachata-manage rebuild  # Пересобрать и перезапустить
```

## Устранение проблем

### Скрипт не запускается

```bash
# Проверьте права
chmod +x /root/install-on-existing-vds.sh

# Запустите с bash (если sh не работает)
bash /root/install-on-existing-vds.sh
```

### Ошибки при установке

Проверьте лог:

```bash
tail -f /var/log/bachata-install.log
```

### MySQL уже установлен

Скрипт проверит существование БД и пользователя, но если возникнут конфликты:

```bash
# Проверьте существующие БД
mysql -e "SHOW DATABASES;"

# Если нужно пересоздать
mysql -e "DROP DATABASE IF EXISTS bachata_db;"
mysql -e "DROP USER IF EXISTS 'bachata_user'@'localhost';"
# Затем запустите скрипт снова
```

### Nginx конфликты

Если Nginx уже настроен:

```bash
# Проверьте существующие конфигурации
ls /etc/nginx/sites-enabled/

# Скрипт создаст новую конфигурацию, но может потребоваться ручная настройка
```

## Отличия от cloud-init

Этот скрипт идентичен cloud-init, но:

- ✅ Работает на уже созданном сервере
- ✅ Можно запускать многократно (с проверками на существующие ресурсы)
- ✅ Показывает пароль БД в консоли (для удобства)
- ✅ Сохраняет лог в `/var/log/bachata-install.log`

## Следующие шаги

После успешной установки:

1. Загрузите файлы проекта
2. Выполните финальные команды
3. Настройте домен (опционально)
4. Установите SSL (опционально)

См. также: [`docs/DEPLOYMENT_VDS.md`](./DEPLOYMENT_VDS.md)
