# Развертывание на VDS Timeweb

Полная инструкция по развертыванию Bachata Beat Counter на VDS сервере с использованием cloud-init.

## Подготовка

### 1. Создание VDS в Timeweb

1. Зайдите в панель управления Timeweb
2. Создайте новый VDS (рекомендуется минимум 4GB RAM, 2 CPU)
3. Выберите ОС: **Ubuntu 22.04 LTS** или **Ubuntu 24.04 LTS**
4. В разделе "User data" или "Cloud-init" вставьте содержимое файла `cloud-init.sh`

### 2. Настройка скрипта перед использованием (опционально)

**Домен не обязателен!** Можно использовать скрипт как есть, и он автоматически определит IP адрес сервера.

Если хотите указать домен сразу, откройте `cloud-init.sh` и измените:

```bash
DOMAIN=""  # Оставьте пустым - будет использован IP (рекомендуется)
# или
DOMAIN="your-domain.com"  # Укажите домен, если уже есть
```

Остальные переменные можно оставить как есть:

```bash
APP_USER="bachata"       # Пользователь приложения
APP_DIR="/opt/bachata"   # Директория приложения
```

## Процесс установки

### Автоматическая установка (через cloud-init)

1. При создании VDS скопируйте содержимое `cloud-init.sh` в поле "User data"
2. Дождитесь завершения установки (10-20 минут)
3. Проверьте логи: `tail -f /var/log/cloud-init.log`

### Ручная установка

Есть два варианта ручной установки:

#### Вариант 1: Автоматический скрипт с проверками (рекомендуется)

Используйте улучшенный скрипт `install-bachata.sh` с автоматическими проверками:

```bash
# Скачайте скрипт
wget https://raw.githubusercontent.com/your-repo/bachata/main/install-bachata.sh -O install-bachata.sh

# Или через curl
curl -o install-bachata.sh https://raw.githubusercontent.com/your-repo/bachata/main/install-bachata.sh

# Сделайте исполняемым и запустите
chmod +x install-bachata.sh
./install-bachata.sh
```

**Преимущества:**
- ✅ Автоматически проверяет, что уже установлено
- ✅ Пропускает выполненные шаги
- ✅ Можно запускать многократно (идемпотентный)
- ✅ Подробное логирование

#### Вариант 2: Оригинальный скрипт

Альтернативно, используйте скрипт `install-on-existing-vds.sh`:

```bash
wget https://raw.githubusercontent.com/your-repo/bachata/main/install-on-existing-vds.sh
chmod +x install-on-existing-vds.sh
sudo ./install-on-existing-vds.sh
```

Подробная инструкция: [`docs/INSTALL_ON_EXISTING_VDS.md`](./INSTALL_ON_EXISTING_VDS.md)

#### Вариант 2: Полностью вручную (пошагово)

Если хотите выполнить установку полностью вручную через консоль, следуйте подробной инструкции:

**📖 [Ручная установка через консоль Таймвеба](./MANUAL_INSTALL_TIMEWEB.md)**

Эта инструкция содержит все команды для пошаговой установки через веб-консоль.

## После установки

### 1. Загрузка проекта на сервер

Есть несколько способов:

#### Вариант A: Через Git (рекомендуется)

```bash
cd /opt/bachata
sudo -u bachata git clone https://github.com/your-username/bachata-beat-counter.git .
```

#### Вариант B: Через SCP/SFTP

```bash
# С вашего компьютера
scp -r ./* root@your-server-ip:/opt/bachata/
ssh root@your-server-ip "chown -R bachata:bachata /opt/bachata"
```

#### Вариант C: Через rsync

```bash
rsync -avz --exclude 'node_modules' --exclude '.next' \
  ./ root@your-server-ip:/opt/bachata/
```

### 2. Настройка переменных окружения

Отредактируйте `.env.local`:

```bash
nano /opt/bachata/.env.local
```

Убедитесь, что указаны правильные значения:

- `DATABASE_URL` - уже настроен автоматически
- `DEMUCS_PYTHON_PATH` - уже настроен автоматически

### 3. Установка зависимостей и сборка

```bash
cd /opt/bachata
sudo -u bachata npm install
sudo -u bachata npm run db:generate
sudo -u bachata npm run db:push
sudo -u bachata npm run build
```

### 4. Загрузка голосовых файлов

Поместите файлы в `/opt/bachata/public/audio/voice/`:

- `1.mp3`, `2.mp3`, `3.mp3`, `4.mp3`, `5.mp3`, `6.mp3`, `7.mp3`, `8.mp3`

**Важно:** Приложение использует только файлы 1-8.mp3 для голосового счета битов.

### 5. Настройка домена и SSL

#### Обновите конфигурацию Nginx:

```bash
nano /etc/nginx/sites-available/bachata-beat-counter
```

Замените `your-domain.com` на ваш реальный домен.

#### Перезагрузите Nginx:

```bash
nginx -t
systemctl reload nginx
```

#### Установите SSL сертификат:

```bash
certbot --nginx -d your-domain.com -d www.your-domain.com
```

## Управление приложением

Используйте скрипт `bachata-manage`:

```bash
# Статус приложения
bachata-manage status

# Просмотр логов
bachata-manage logs

# Перезапуск
bachata-manage restart

# Пересборка после изменений
bachata-manage rebuild
```

## Проверка работы

### 1. Проверка сервиса

```bash
systemctl status bachata-beat-counter
```

### 2. Проверка логов

```bash
journalctl -u bachata-beat-counter -f
```

### 3. Проверка базы данных

```bash
mysql -u bachata_user -p bachata_db
# Пароль находится в /root/db_credentials.txt
```

### 4. Проверка Python окружения

```bash
sudo -u bachata /opt/bachata/venv/bin/python -c "from madmom.features import RNNDownBeatProcessor; print('Madmom OK')"
```

## Обновление приложения

### Через Git:

```bash
cd /opt/bachata
sudo -u bachata git pull
bachata-manage update
```

### Вручную:

```bash
cd /opt/bachata
sudo -u bachata npm install
sudo -u bachata npm run build
bachata-manage restart
```

## Резервное копирование

### База данных:

```bash
# Создание бэкапа
mysqldump -u bachata_user -p bachata_db > backup_$(date +%Y%m%d).sql

# Восстановление
mysql -u bachata_user -p bachata_db < backup_20240101.sql
```

### Файлы загрузок:

```bash
tar -czf uploads_backup_$(date +%Y%m%d).tar.gz /opt/bachata/public/uploads/
```

## Мониторинг

### Использование ресурсов:

```bash
# CPU и память
htop

# Дисковое пространство
df -h

# Логи приложения
journalctl -u bachata-beat-counter --since "1 hour ago"
```

## Устранение проблем

### Приложение не запускается

1. Проверьте логи: `bachata-manage logs`
2. Проверьте статус: `bachata-manage status`
3. Проверьте порт: `netstat -tlnp | grep 3000`

### Ошибки базы данных

1. Проверьте подключение: `mysql -u bachata_user -p bachata_db`
2. Проверьте `.env.local`: `cat /opt/bachata/.env.local`
3. Пересоздайте БД: `npm run db:push`

### Ошибки madmom

1. Проверьте установку: `/opt/bachata/venv/bin/python -c "import madmom"`
2. Переустановите: `/opt/bachata/venv/bin/pip install --force-reinstall madmom`

### Nginx не проксирует

1. Проверьте конфигурацию: `nginx -t`
2. Проверьте логи: `tail -f /var/log/nginx/bachata-beat-counter-error.log`
3. Перезагрузите: `systemctl reload nginx`

## Безопасность

### Рекомендации:

1. **Измените пароль БД** после первой установки
2. **Настройте firewall** (уже настроен в скрипте)
3. **Используйте SSH ключи** вместо паролей
4. **Регулярно обновляйте систему**: `apt-get update && apt-get upgrade`
5. **Настройте автоматические бэкапы** БД

### Изменение пароля БД:

```bash
mysql -e "ALTER USER 'bachata_user'@'localhost' IDENTIFIED BY 'new_secure_password';"
# Обновите DATABASE_URL в .env.local
nano /opt/bachata/.env.local
```

## Производительность

### Оптимизация для больших файлов:

В `/etc/nginx/nginx.conf` добавьте:

```nginx
http {
    client_max_body_size 200M;
    client_body_timeout 600s;
    proxy_read_timeout 600s;
}
```

### Оптимизация Node.js:

В systemd сервисе можно добавить:

```ini
Environment="NODE_OPTIONS=--max-old-space-size=4096"
```

## Быстрый старт

Для быстрой установки через консоль см. [`docs/QUICK_START.md`](./QUICK_START.md)

## Поддержка

При возникновении проблем проверьте:

- Логи установки: `/var/log/bachata-install.log`
- Логи cloud-init: `/var/log/cloud-init.log`
- Логи приложения: `journalctl -u bachata-beat-counter`
- Логи Nginx: `/var/log/nginx/bachata-beat-counter-error.log`
